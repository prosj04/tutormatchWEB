# 리팩토링 제안 (Refactoring Plan)

> 작성: 2026-07-04 · 근거: `internal/TECH_OVERVIEW.md` §8 기술부채 + 2026-07-04 코드 실측(파일 크기·인증 패턴 grep).
> 원칙: 파일럿 개시 전에는 **보안·데이터 정합성(P0~P1)만** 처리하고, 구조 개선(P2)은 기능 개발과 충돌하지 않는 시점에 점진 수행. 리팩토링을 위한 리팩토링은 하지 않는다.

---

## P0 — 보안 (파일럿 개시 전 필수)

### R-1 · Toss 웹훅 서명 검증 부재 🔴
- **현황**: 결제 웹훅 수신 시 서명 검증 없음 (TECH_OVERVIEW §8). 위조 요청으로 결제 상태를 조작할 수 있는 경로.
- **제안**: 웹훅 라우트에 Toss 서명 헤더 검증 추가 + 실패 시 401. 검증 로직은 `src/lib/`에 단일 헬퍼로.
- **규모**: 소 (반나절). 실 결제가 발생하는 파일럿 전 **반드시** 완료.

### R-2 · Supabase RLS 미설정
- **현황**: DB 접근이 Prisma(서비스 롤) 단일 경로라 당장 노출은 없으나, anon 키가 클라이언트에 존재하는 이상 RLS 부재는 방어선 0개.
- **제안**: 최소한 전 테이블 `DENY ALL` 기본 정책 적용(Prisma는 서비스 롤이므로 영향 없음). 테이블별 정밀 정책은 모바일 앱이 Supabase 직접 접근을 쓰게 될 때 재설계.
- **규모**: 소 (SQL 마이그레이션 1건).

### R-3 · CHIEF_MANAGER 미들웨어 경로 검증 [확인 필요]
- **현황**: TECH_OVERVIEW §8에 "미확인"으로 기재. API 레이어는 `requireChiefManagerOrAdmin`(`src/lib/admin-auth.ts`)으로 보호되나, **페이지 미들웨어**가 CHIEF_MANAGER 전용 경로를 올바르게 게이트하는지 미검증.
- **제안**: 확인 태스크 — `middleware.ts`의 role 분기 표를 만들어 4역할×주요 경로 매트릭스로 검증. 구멍 발견 시에만 수정.
- **규모**: 소 (확인 2시간).

## P1 — 데이터 정합성·안정성

### R-4 · QnA 모델 이중화 해소 (`Question` → `QuestionMessage`)
- **현황**: 구 `Question` 모델과 신 `QuestionMessage` 모델이 공존 (TECH_OVERVIEW §8). 읽기·쓰기 경로가 갈라져 있어 신규 기능마다 분기 비용 발생.
- **제안**: ① 구 데이터 → 신 모델 백필 마이그레이션 ② 읽기 경로 통일 ③ 구 모델 쓰기 차단 ④ 한 릴리스 뒤 모델 제거. 데이터가 적은 지금(파일럿 전)이 마이그레이션 최적기.
- **규모**: 중 (1~2일).

### R-5 · Journey 상태 드리프트 (`FIRST_LESSON_PENDING`)
- **현황**: 학생 여정 상태가 실제 진행과 어긋나는 드리프트 존재 (TECH_OVERVIEW §8). 매니저 화면의 신뢰도 문제 — 운영이 시작되면 매니저가 잘못된 상태를 보고 행동하게 됨.
- **제안**: 상태 전이를 단일 함수로 수렴(전이 규칙 표 기반), 드리프트 검출 배치를 `run-alert-checks`에 추가.
- **규모**: 중.

### R-6 · `connection_limit=1` 병렬 쿼리 제약
- **현황**: Prisma 연결 1개 제약으로 `Promise.all` 병렬 쿼리 금지가 **암묵 규약**으로만 존재. 위반해도 로컬에서는 통과하고 프로덕션에서만 간헐 실패하는 최악의 부채 유형.
- **제안**: ① `src/app/api` 전수 grep으로 현존 위반 감사 ② ESLint 커스텀 룰 또는 최소한 CLAUDE.md/AGENTS.md에 규약 명문화 ③ 중기적으로 Supabase pooler(pgbouncer) 경유로 limit 상향 검토.
- **규모**: 감사 소 / 근본 해소 중.

### R-7 · 정산 시급 상수의 데이터 이관
- **현황**: `src/lib/settlement.ts`의 `TEACHER_HOURLY_RATE_KRW` 하드코딩. 현재는 단순함이 미덕이지만, `BM_EVOLUTION.md` BM-A(등급별 차등 시급)가 채택되면 즉시 병목.
- **제안**: 지금은 유지. BM-A 착수 결정 시 Teacher 레코드 필드(+변경 이력 테이블)로 이관. **선제 구현 금지** — BM 결정에 종속.
- **규모**: 중 (BM-A 채택 시).

## P2 — 유지보수성 (점진 수행)

### R-8 · 대형 파일 분할 (실측 상위)

| 파일 | 줄수 | 분할 제안 |
|---|---|---|
| `src/components/admin/AdminCmsPage.tsx` | 2,545 | 섹션 에디터별 컴포넌트 분리 (히어로/후기/FAQ/요금 등) — CMS 섹션 추가 때마다 이 파일이 자라는 구조를 차단 |
| `scripts/run-alert-checks.ts` | 1,806 | 체크 1개=모듈 1개 레지스트리 패턴 — R-5 드리프트 검출 추가 시 함께 |
| `src/lib/cms-page-defaults.ts` | 1,080 | 페이지별 defaults 파일로 분리 (데이터 파일이라 우선순위 낮음) |
| `src/components/landing/LandingPageV2.tsx` | 802 | 섹션 컴포넌트 분리 — 마케팅 반복 수정(MK 백로그)이 집중되는 파일이라 실익 큼 |
| `src/components/manager/ManagerConsultationsPage.tsx` | 750 | 리스트/상세/배정 모달 분리 |
| `src/components/admin/AdminTeachersPage.tsx` | 705 | 상동 패턴 |
| `src/app/register/teacher/page.tsx` | 658 | 스텝별 폼 컴포넌트 분리 |

- **원칙**: 기능 수정 차 열게 될 때 그 파일만 분할(보이스카우트 룰). 일괄 분할 스프린트는 회귀 리스크 대비 실익 없음.

### R-9 · 합성 이메일 `*@concord.local`
- **현황**: 전화 가입자에게 합성 이메일 부여. 알림 발송·중복 검사에서 특수 처리 필요한 암묵 규칙.
- **제안**: `email` nullable 전환 + `phone`을 1급 식별자로 승격이 정도(正道)이나 마이그레이션 부담 큼. 단기: 합성 이메일 판별 헬퍼(`isSyntheticEmail`) 하나로 산재한 판별 로직 수렴 + 이메일 발송 전 필터 일원화.
- **규모**: 단기 소 / 근본 대.

### R-10 · 인증 가드 헬퍼 통합 (저순위 — 현재 패턴 양호)
- **현황(실측)**: 역할별 가드가 `admin-auth.ts`(44줄)·`manager-auth.ts`·`teacher-auth.ts`·`student-auth.ts` 등 7개 파일 총 313줄로 분리. 401/403 분기 보일러플레이트가 파일마다 반복되지만 각 파일이 작고 명확해 **문제라고 보기 어려움**.
- **제안**: 신규 역할 추가 시점에만 `requireRole(roles, options)` 제네릭으로 수렴 검토. 그 전에는 손대지 않는다.

### R-11 · CMS 캐시 5분 지연
- **현황**: CMS 수정 후 반영까지 최대 5분. 운영자 혼란 요인.
- **제안**: CMS 저장 시 `revalidateTag` 온디맨드 무효화 추가 (Next.js 표준 패턴). 캐시 TTL 자체는 유지.
- **규모**: 소.

---

## 실행 순서 요약

| 순위 | 항목 | 시점 |
|---|---|---|
| 1 | R-1 웹훅 서명 · R-2 RLS 기본 정책 · R-3 미들웨어 검증 | **파일럿 개시 전** (합계 1~2일) |
| 2 | R-4 QnA 마이그레이션 · R-6 병렬 쿼리 감사 | 파일럿 전 데이터 적을 때 |
| 3 | R-5 Journey 드리프트 · R-11 CMS 무효화 | 운영 개시 직후 (매니저 경험 직결) |
| 4 | R-8 파일 분할 (터치 시) · R-9 합성 이메일 단기안 | 상시 점진 |
| 5 | R-7 시급 데이터 이관 · R-10 가드 통합 | 각각 BM-A 채택 · 신규 역할 추가 시 |

# Concord 프론트엔드 재구축 명세 (FRONTEND BUILD SPEC)

> **⚠️ 이 파일은 포인터 스텁이다 (2026-07-12 중복 단일화).**
>
> 원본은 [`design handoff/FRONTEND_BUILD_SPEC.md`](../design%20handoff/FRONTEND_BUILD_SPEC.md) — 디자인 핸드오프 패키지의 일부로 그쪽이 유일한 진실의 원천이다.
>
> 배경: 07-10~07-11에 `docs/`와 `design handoff/` 양쪽에 바이트 단위 동일한 사본이 존재했다. 한쪽만 수정되면 어긋나므로 07-12 문서 정비에서 `design handoff/` 쪽을 원본으로 확정하고 이 파일을 스텁으로 교체했다. 내용 손실 없음(교체 시점에 두 파일이 완전 동일함을 diff로 확인).
>
> 함께 볼 것: `design handoff/IMPLEMENTATION_CONTRACT.md`(구현 계약), `design handoff/ROUTE_MAP.md`(시안↔라우트 매핑), `design handoff/DESIGN_SYSTEM.md`(토큰·타이포).
